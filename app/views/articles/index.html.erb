<% @articles.each do |article| %>
	<h1><%= link_to article.title, article %></h1>
	<div>
		<%= article.body %> <% if user_signed_in? %>
		<%= link_to "- Eliminar", article, method: :delete, class:"red" %>
		<% end %>
	</div>
<% end %>
